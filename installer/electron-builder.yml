appId: com.rase.setup
productName: Rase Setup
compression: store
directories:
  output: dist-package
files:
  - dist/**/*
  - dist-electron/**/*
  - bundle/**/*
  - package.json
extraResources:
  - bundle
win:
  target:
    - portable
portable:
  artifactName: "${productName}-${version}.${ext}"
